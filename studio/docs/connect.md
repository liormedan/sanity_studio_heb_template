## חיבור ל‑Sanity

ערכו את קובץ `.env` תחת `studio/.env` וקבעו:

```
SANITY_STUDIO_PROJECT_ID=yourProjectId
SANITY_STUDIO_DATASET=production
```

אפשר להשתמש גם בכלי "התחלה מהירה" כדי להעתיק ערכי .env או לבצע Provision אוטומטי.

